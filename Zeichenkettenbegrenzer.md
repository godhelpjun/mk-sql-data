Die Begrenzer umschließen Zeichenketten und können aus einem Paar der Zeichen  " oder ' oder ` bestehen.

## Beispiel
```
   set "char_field" to `another string`
   set "char_field" to 'another string'
```